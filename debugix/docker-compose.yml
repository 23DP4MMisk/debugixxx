services:
  debugix:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - debugix-data:/data
    environment:
      - PORT=3000
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  debugix-data:
